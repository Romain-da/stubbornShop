{% extends 'base.html.twig' %}

{% block title %}Accueil - Stubborn{% endblock %}

{% block body %}
<div class="container">
    <h1 class="text-center my-4">Bienvenue chez Stubborn</h1>

    <p class="text-center">Découvrez nos produits de qualité et notre engagement envers la satisfaction de nos clients.</p>
    
    <div class="row">
        {% for product in featured_products %}
            <div class="col-md-4">
                <div class="card mb-4">
                    <img src="{{ asset('images/' ~ product.image) }}" class="card-img-top" alt="{{ product.name }}">
                    <div class="card-body text-center">
                        <h5 class="card-title">{{ product.name }}</h5>
                        <p class="card-text">{{ product.price }} €</p>
                        <a href="{{ path('app_product_show', {'id': product.id}) }}" class="btn btn-primary">Voir</a>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>

    <div class="text-center mt-5">
        <h2>Notre Histoire</h2>
        <p>Stubborn est une marque dédiée à la qualité et à l'innovation. Découvrez nos produits et rejoignez notre communauté.</p>
    </div>
</div>
{% endblock %}
